print("What's your name?");
(let name = readline() in
  let prefix = name[0..3] in
     if(equal(prefix, "mic") || equal(prefix, "Mic")):
       print("{name}, you have an awesome name!")
     else:
       print("{name} is a terrible name."));
"Thanks for playing!"
